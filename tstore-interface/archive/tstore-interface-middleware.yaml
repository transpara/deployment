apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.tStoreInterface.name }}-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - "{{ .Values.tStoreInterface.config.BASE_PATH }}"
