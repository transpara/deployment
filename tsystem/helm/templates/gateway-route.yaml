apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: tsystem-api-route
  namespace: default
spec:
  parentRefs:
  - name: tgateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: "{{ .Values.tSystemUi.config.BASE_PATH }}"
    backendRefs:
    - name: tsystem-ui
      port: 3000
  - matches:
    - path:
        type: PathPrefix
        value: "{{ .Values.tSystemApi.config.BASE_PATH }}"
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: "/"
    backendRefs:
    - name: tsystem-api
      port: 80
  - matches:
    - path:
        type: PathPrefix
        value: "/tsystem"
    filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: "http"
        path:
          type: "ReplacePrefixMatch"
          replacePrefixMatch: "/tsystem/ui"