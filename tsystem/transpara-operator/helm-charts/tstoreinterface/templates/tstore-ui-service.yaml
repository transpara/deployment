apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tStoreUi.name }}
spec:
  selector:
    app: {{ .Values.tStoreUi.name }}
  type: {{ .Values.tStoreUi.service.type }}
  ports:
{{ toYaml .Values.tStoreUi.service.ports | indent 4 }}
