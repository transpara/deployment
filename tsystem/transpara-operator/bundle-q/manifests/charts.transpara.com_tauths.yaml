apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: tauths.charts.transpara.com # Adjusted from tsystems.charts.transpara.com to tauths.charts.transpara.com
spec:
  group: charts.transpara.com
  names:
    kind: TAuth # Adjusted from TSystem to TAuth
    listKind: TAuthList # Adjusted from TSystemList to TAuthList
    plural: tauths # Adjusted from tsystems to tauths
    singular: tauth # Adjusted from tsystem to tauth
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: TAuth is the Schema for the tauths API # Adjusted description to match TAuth
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of TAuth 
            type: object
            x-kubernetes-preserve-unknown-fields: true
          status:
            description: Status defines the observed state of TAuth
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
