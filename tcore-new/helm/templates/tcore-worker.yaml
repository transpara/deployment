apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.tcoreWorker.deployment.name }}
spec:
  replicas: {{ .Values.tcoreWorker.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.tcoreWorker.deployment.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.tcoreWorker.deployment.name }}
    spec:
      containers:
      - name: {{ .Values.tcoreWorker.deployment.name }}
        image: "{{ .Values.tcoreWorker.image.repository }}:{{ .Values.tcoreWorker.image.tag }}"
        command: ["/app/run-celeryworker.sh"]
        env:
        - name: TCORE_NAME
          value: {{ .Values.tcoreWorker.config.TCORE_NAME }}
        - name: TGRAPH_HOST
          value: {{ .Values.tcoreWorker.config.TGRAPH_HOST }}
        - name: TGRAPH_PORT
          value: "{{ .Values.tcoreWorker.config.TGRAPH_PORT }}"
        - name: CELERY_BROKER_URL
          value: "redis://{{ .Values.tcoreWorker.config.REDIS_HOST }}:{{ .Values.tcoreWorker.config.REDIS_PORT }}/0"
        - name: CELERY_RESULT_BACKEND
          value: "redis://{{ .Values.tcoreWorker.config.REDIS_HOST }}:{{ .Values.tcoreWorker.config.REDIS_PORT }}/1"
        - name: REDIS_HOST
          value: {{ .Values.tcoreWorker.config.REDIS_HOST }}
        - name: REDIS_PORT
          value: "{{ .Values.tcoreWorker.config.REDIS_PORT }}"
        - name: TAUTH_HOST
          value: {{ .Values.tcoreWorker.config.TAUTH_HOST }}
        - name: TAUTH_PORT
          value: "{{ .Values.tcoreWorker.config.TAUTH_PORT }}"
        - name: TCORE_ID
          value: {{ .Values.tcoreWorker.config.TCORE_ID }}
        - name: OTEL_EXPORTER_HOST
          value: {{ .Values.tcoreWorker.config.OTEL_EXPORTER_HOST }}
        # Additional environment variables can be added as needed
